# Atoms Demo 需求分析计划

## 1. 目标与产出

本计划用于把 `requirement.txt` 中较高层的挑战说明，收敛为可实现、可验收、可上线演示的产品需求，再开始界面和代码开发。

分析完成后应产出：

- 一份确认过范围的 MVP 功能清单与非目标；
- 一条可端到端演示的用户旅程；
- 页面/交互清单、数据模型和持久化策略；
- 技术选型与“真实生成”实现方案；
- 可测试的验收用例、风险清单和上线检查项。

## 2. 已知约束与需求分层

### P0：必须满足

| 编号 | 需求 | 可验收结果 |
| --- | --- | --- |
| R1 | 构建一个可运行的网页应用，呈现类似 Atoms 的 AI Agent 交互体验 | 本地启动后可完整操作；部署后有可访问链接 |
| R2 | 用户可用自然语言驱动智能体生成代码/应用 | 输入需求后，系统产生可查看的应用产物，而非仅展示预置静态页面 |
| R3 | 生成结果须以可视化网页预览展示 | 用户能在同一项目中打开预览，并看到生成应用的实际交互 |
| R4 | 具备真实交互 | 至少包括输入、提交、生成进度、项目切换/打开、预览或迭代等可操作反馈 |
| R5 | 具备数据持久化 | 刷新页面或重新打开应用后，项目、提示词、生成结果及必要状态仍可恢复 |
| R6 | 提供基础使用流程 | 覆盖初始化/注册（或等价的首次使用身份建立）和核心生成流程 |
| R7 | 可实际使用，不只是概念验证 | 成功、失败、空状态和恢复路径都有明确反馈；关键流程可重复使用 |
| R8 | 提供清晰交付文档 | 含启动、测试、部署、已实现范围及限制说明 |

### P1：应尽量覆盖

| 编号 | 需求 | 候选实现 |
| --- | --- | --- |
| R9 | 至少一个延展/衍生能力 | 生成后的自然语言迭代、版本历史/回滚、模板库、分享链接或生成记录搜索（优先选一个） |
| R10 | 体现 Agent 能力与 UI 体验 | 显示任务阶段、智能体工作日志/计划、代码变更摘要和可读的生成状态 |
| R11 | 满足工程质量与可交付性 | 基础错误处理、输入校验、持久化迁移策略、自动化测试及部署检查 |

### 非目标（首版不承诺）

- 不复刻 Atoms 的全部多智能体、赛马、深度研究或社区功能；
- 不承诺生成任意复杂生产级应用；
- 不将真实模型密钥暴露到浏览器，也不以伪造“生成完成”替代真实可执行产物；
- 若时间有限，不同时实现账号体系、多人协作、复杂发布管理和多模型比较。

## 3. 产品理解与分析假设

Atoms 的核心不是聊天界面本身，而是 **用户表达意图 → 智能体执行 → 人审阅结果 → 继续迭代** 的 Vibe Coding 闭环。官方术语页中“App 预览”“人工介入机制”“迭代”“发布”均与本 Demo 高度相关。

首版暂定以下假设，待需求澄清时确认：

1. 目标用户是希望快速制作小型网页应用的个人用户，以及评估完成度的评审者。
2. “注册”可采用轻量的名称/邮箱初始化或本地访客工作区；若没有后端认证条件，不强制接入真实邮件验证。
3. “生成应用”应根据用户自然语言生成受约束的 HTML/CSS/JS 单页应用，并在沙箱预览中运行；模型不可用时需有明确失败反馈，而非静默失败。
4. 单用户、单浏览器持久化可以作为 MVP；若部署环境支持，则优先使用服务端数据库以便分享和跨设备恢复。

### 3.1 已确认的产品决策（2026-08-18）

| 决策 | 已确认内容 | 对实现的影响 |
| --- | --- | --- |
| 模型调用 | 支持部署者默认配置，也支持用户覆盖 endpoint、model 和 API Key | 默认配置来自 `.env`；覆盖值只保存在当前标签页并随单次生成请求发送。用户填写 endpoint 时必须同时提供自己的 model 和 Key，服务端默认 Key 不流向用户 endpoint |
| 生成范围 | 限定为可稳定生成的小型单页应用，但不限制为固定模板 | 使用受限 HTML/CSS/JS 文件集合、代码大小/能力白名单和 sandbox 预览保证稳定性 |
| 初始化/注册 | 不需要真实邮箱注册 | 使用本地昵称/访客工作区完成首次初始化 |
| 延展能力 | 版本历史与回滚 | 每次成功生成保存版本，可比较并恢复至旧版本 |
| 交付顺序 | 先完成本地 Demo 并验收 | 先不引入线上部署、分享链接和跨设备同步的复杂度 |

这些决策已覆盖正式开发前会改变架构的关键问题；具体技术栈、文件约束和视觉细节由实现阶段在不突破上述边界的前提下确定。

## 4. 需求分析工作流

| 阶段 | 要回答的问题 | 方法 | 产出/退出条件 |
| --- | --- | --- | --- |
| A. 需求拆解 | 哪些是硬性验收项，哪些是加分项？ | 将原文逐条映射为 R1–R11；标注依赖与优先级 | 需求追踪矩阵，无遗漏的 P0 清单 |
| B. 用户与场景 | 谁在什么场景下使用，成功是什么样？ | 定义用户角色、任务目标、痛点和成功信号 | 2 个角色、1 条主旅程、1 条异常/恢复旅程 |
| C. 流程与状态 | 从首次进入到生成、预览、迭代、重开项目如何流转？ | 绘制用户流程与状态机；列出加载、失败、空状态 | 页面地图、状态转换、交互文案原则 |
| D. 功能范围 | MVP 的最小闭环是什么，延展能力选哪一个？ | MoSCoW 优先级；以演示价值/实现成本评估 | MVP、P1、非目标三张清单；一个已选扩展能力 |
| E. 方案可行性 | 如何使“生成”真实、预览安全、数据可恢复？ | 比较生成引擎、预览隔离、存储和部署方案 | 技术决策记录（ADR）及降级方案 |
| F. 数据与权限 | 要保存什么，谁可访问，如何恢复？ | 建模实体、生命周期、删除/更新规则 | 数据字典、持久化边界、隐私约束 |
| G. 验收与风险 | 怎样证明它可用，哪里最容易失败？ | 编写 Given–When–Then 用例；风险评分 | 验收清单、测试计划、风险及缓解措施 |

## 5. 拟验证的核心用户旅程

```text
首次访问
  → 初始化身份/工作区
  → 创建项目并描述想要的应用
  → 智能体展示计划与生成进度
  → 生成可运行的应用文件/产物
  → 在 App 预览中操作生成应用
  → 提出修改请求并再次生成
  → 自动保存，刷新或稍后重新打开项目
  → （延展）查看版本/恢复版本 或 获得分享链接
```

必须为下列分支定义行为和文案：

- 空输入、超长输入、重复提交；
- 生成进行中再次编辑或切换项目；
- 生成失败、网络/模型不可用、生成内容不合法；
- 首次无项目、持久化数据不存在或损坏；
- 预览加载失败、用户刷新页面后恢复到哪个状态；
- 受限脚本/不安全内容不能直接执行时的安全提示。

## 6. 功能需求初稿

### 6.1 工作区与项目

- 首次进入完成轻量初始化，建立当前用户/访客工作区。
- 可新建项目、命名项目、查看最近项目并重新打开。
- 项目保存提示词会话、生成版本、预览入口和更新时间。

### 6.2 Agent 生成工作台

- 提供清晰的任务输入区，支持描述页面、功能和视觉偏好。
- 提交后显示可理解的阶段，例如“分析需求 → 生成应用 → 校验 → 准备预览”。
- 显示本次生成的计划、摘要和结果，避免把 Agent 过程完全黑箱化。
- 允许基于当前项目发送迭代指令，例如“将按钮改成深色，并增加删除功能”。
- 同一请求在完成、失败、取消或重试时状态明确，且不丢失原始输入。

### 6.3 应用预览

- 生成完成后自动或一键打开预览。
- 预览内容与生成版本对应，并有加载、错误和空状态。
- 预览与工作台并置或可快速切换；用户可回到对话继续修改。
- 预览在隔离上下文中执行，以避免生成代码污染主应用。

### 6.4 持久化与延展能力

- P0 持久化：工作区基本资料、项目元数据、消息/提示词、版本产物、当前选中项目。
- 推荐 P1：**版本历史与回滚**。它同时体现人工审阅、迭代和工程思维，且能直观证明数据持久化。
- 可选 P1 替代：为项目创建只读分享链接；仅在数据后端和部署时间允许时采用。

## 7. 页面与交互分析清单

| 页面/区域 | 核心目的 | 最小交互 | 关键状态 |
| --- | --- | --- | --- |
| 欢迎/初始化 | 建立身份和首次目标 | 输入名称/创建工作区/开始第一个项目 | 未初始化、校验失败、已完成 |
| 项目列表 | 找回和管理历史项目 | 新建、选择、重命名（可选） | 空列表、加载中、选中态 |
| 生成工作台 | 描述需求并观察 Agent 工作 | 输入、提交、重试、继续迭代 | 待输入、生成中、成功、失败、取消 |
| Agent 过程面板 | 让过程可信、可审阅 | 展开计划/日志/文件摘要 | 进行中、完成、有警告 |
| App 预览 | 直接验证生成结果 | 刷新预览、打开版本、返回编辑 | 加载中、运行、运行错误、无产物 |
| 版本记录（P1） | 比较并恢复历史生成 | 查看、切换、恢复 | 无历史、当前版本、恢复确认 |

## 8. 数据与持久化分析

初步实体关系：

```text
Workspace/User 1 ── * Project 1 ── * ConversationMessage
                             └── * GenerationVersion 1 ── 1 PreviewArtifact
```

| 实体 | 最小字段 | 生命周期 |
| --- | --- | --- |
| Workspace/User | id、displayName、createdAt | 初始化时创建；可本地恢复 |
| Project | id、name、promptSummary、createdAt、updatedAt、selectedVersionId | 创建、编辑、归档（可选） |
| ConversationMessage | id、projectId、role、content、createdAt、status | 每次输入和 Agent 回复追加，失败保留记录 |
| GenerationVersion | id、projectId、sequence、plan、summary、status、createdAt | 每次成功/失败生成均可追溯 |
| PreviewArtifact | versionId、html/css/js（或文件集合）、validationResult | 仅在校验通过后标记为可预览 |

分析阶段需要作出的决策：

1. **存储位置**：优先评估 SQLite/服务端数据库；若项目规模或部署限制不适合，使用 IndexedDB/localStorage，并在 README 明确单设备限制。
2. **生成产物形态**：优先选择受控的单页 HTML/CSS/JS 或受控组件 schema，降低任意代码执行的风险与复杂度。
3. **预览隔离**：使用 `iframe sandbox` / 独立 preview 路由，禁止生成物访问主应用敏感数据。
4. **数据恢复**：启动时恢复最近项目和最后成功预览；生成中断后保留“失败/可重试”记录。

## 9. 技术可行性决策点（开发前确认）

| 决策 | 备选方案 | 推荐判断标准 |
| --- | --- | --- |
| 生成引擎 | 真实 LLM API；受限模板 + 规则生成；二者混合 | 必须能重复产出可运行应用；密钥安全；演示环境稳定 |
| 生成范围 | 任意前端代码；受约束的通用小型单页文件集合 | 以可用性和安全优先，限制文件、大小、API 能力和外部资源，而不限制应用领域 |
| 数据库 | 浏览器本地；SQLite；托管数据库 | 部署复杂度、跨设备需求、分享能力和恢复可靠性 |
| 身份方式 | 匿名工作区；本地昵称；真实认证 | 只实现满足“初始化/注册”且不影响演示可靠性的最轻方案 |
| 发布方式 | 静态站点；全栈托管；容器 | 是否需要后端/API、环境变量保护、稳定线上链接 |

任何涉及外部模型 API 的方案都要同时定义：密钥存放位置、额度/超时、错误反馈和速率限制。当前 Demo 无共享 Key 或伪生成降级；用户未配置模型时明确提示，模型失败时保留请求并允许重试。

## 10. 验收标准与测试计划

### 端到端验收场景

1. 新用户首次进入，完成初始化后可以创建项目并到达生成工作台。
2. 输入一个明确需求（例如“生成一个可开始、暂停和重置的番茄钟”），系统展示阶段状态并生成可操作的预览。
3. 在预览中实际使用生成应用的核心交互；不是仅看到截图或文本描述。
4. 对当前项目发送修改需求，系统产生新版本，预览切换到新结果。
5. 刷新浏览器或重新启动应用后，能找回该项目、对话记录和最后成功版本。
6. 模型/网络/校验失败时，用户看到原因和重试入口，历史数据仍保留。
7. （P1）从版本历史恢复旧版本，预览与数据状态一致；或成功生成可访问的分享链接。
8. 部署环境中完成上述 P0 场景，并从无痕窗口访问线上链接验证。

### 质量门槛

- P0 流程无阻塞性错误；浏览器控制台无未处理异常。
- 所有异步动作都有加载、防重复提交和失败反馈。
- 关键状态持久化有自动化测试或可复现的手工测试脚本。
- README 说明一条命令启动方式、环境变量、测试方式、部署地址和已知限制。

## 11. 风险与缓解

| 风险 | 影响 | 缓解方案 |
| --- | --- | --- |
| LLM 输出不稳定或不可执行 | 主流程无法演示 | 限制生成范围；加入格式校验和修复重试；准备可声明的本地降级生成器 |
| 在主页面直接执行生成代码 | 安全及稳定性风险 | 隔离预览；限制脚本能力；不暴露主应用数据与密钥 |
| 只做了聊天 UI，未形成产物闭环 | 不满足核心要求 | 把“可运行预览”和“持久化重开”列为不可拆分的 P0 验收项 |
| 将功能铺得过宽 | 影响完成度和稳定性 | 先锁定一条端到端旅程，P1 仅选一个延展能力 |
| 本地持久化与线上分享目标冲突 | 交付体验受限 | 在架构决策阶段确认是否需要后端；不需要则明确单设备边界 |
| 部署或 API 配置临近交付才发现问题 | 无在线链接或线上不可用 | 在 MVP 跑通后尽早部署一次，并用生产环境做回归测试 |

## 12. 已关闭的需求确认项

正式开发前的关键决策均已确认：使用项目环境变量调用真实模型、限制生成范围、采用轻量本地初始化、实现版本历史/回滚，并先以本地验收为目标。

实现过程中若发现 API 协议与 OpenAI 兼容接口存在差异，或某项受控生成范围不能稳定产出可运行预览，将先给出替代方案和影响说明，再扩展范围。

## 13. 建议的后续执行顺序

1. 确认第 12 节的决策，锁定 MVP 和唯一的 P1 延展能力。
2. 输出简版 PRD：用户故事、流程图、页面线框、状态文案和验收用例。
3. 完成技术 spike：验证一次“输入 → 生成受控应用 → 沙箱预览 → 保存/恢复”的最短链路。
4. 以该链路为骨架实现工作区、项目、Agent 工作台和预览；先满足 R1–R8。
5. 增加版本历史/回滚（或已确认的替代 P1），补齐异常路径和测试。
6. 部署、线上回归、补齐 README 与演示脚本，最终逐条核对 R1–R11。

## 14. 完成定义（Definition of Ready）

在开始正式开发前，以下条件全部满足即视为需求分析完成：

- [ ] P0/P1/非目标已确认；
- [x] 已选定“受约束的通用小型单页应用”生成范围；
- [ ] 主用户旅程、异常旅程和页面清单已确认；
- [ ] 生成、预览隔离、持久化、身份和部署方案已做出决策；
- [ ] 数据字段、状态机和错误处理边界已明确；
- [ ] P0 验收用例可被逐条执行；
- [ ] README 的交付内容和线上访问方式已列入开发计划。
